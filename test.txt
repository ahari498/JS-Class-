echo "# JS-Class-" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/ahari498/JS-Class-.git
git push -u origin master


second time.

git add. 
git commit -m"first commit"
git push

Creating New branch'

git checkout -b myhomepage


To clone\
git clone url
git pull (before every request)

moving to different branch

git checkout -b master

 